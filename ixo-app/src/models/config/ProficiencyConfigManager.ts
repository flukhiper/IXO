import type { ProficiencyConfig } from '@/types/config/proficiency';
import { BaseConfigManager } from './BaseConfigManager';

export class ProficiencyConfigManager extends BaseConfigManager<ProficiencyConfig> {
  constructor () {
    super();
  }
} 