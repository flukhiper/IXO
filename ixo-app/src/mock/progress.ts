// eslint-disable-next-line @typescript-eslint/no-unused-vars
const mockProgress = {
  1: {
    gains: [
      {
        type: 'learn' // one of ['skill', 'action', 'learn']. 'skill' mean acquire skill when level reach. 'action' mean acquire action when level reach. 'learn' mean choose a skill from pool that created from list all skills that requirement matched.
      }
    ]
  }
};