export interface DamageScaleConfig {
  id: string;
  name: string;
  description?: string;
  value: number;
}

export interface DamageTypeConfig {
  id: string;
  name: string;
  description?: string;
}